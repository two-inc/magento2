name: playwright

# Runs the Playwright suite that drives the plugin on the dev store and captures
# the docs screenshots (admin config tabs + storefront checkout journey). The
# checkout journey runs with no credentials; the admin-gated specs (admin config,
# minimum-order gate) need a Magento admin password (ADMIN_PASS) and skip cleanly
# without it.

permissions:
  contents: read
  id-token: write

on:
  workflow_dispatch:
    inputs:
      store_url:
        description: "Storefront base URL"
        required: false
        default: "https://magento-dev.staging.two.inc"
      grep:
        description: "Only run tests whose title matches (blank = all)"
        required: false
        default: ""
  # Only trigger on changes to the suite itself. The tests run against the dev
  # store, which git-syncs `staging` — so a PR's own plugin changes aren't live
  # yet at PR time and can't be meaningfully exercised here; scoping to e2e/**
  # validates test changes against deployed `staging` without gating undeployed
  # plugin code.
  pull_request:
    paths:
      - "e2e/**"
      - ".github/workflows/playwright.yml"

# One global group: the minimum-order spec temporarily rewrites shared dev
# store config (and restores it), so two runs must never overlap. Queue rather
# than cancel — cancelling a run mid-test could skip the config restore.
concurrency:
  group: playwright
  cancel-in-progress: false

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: e2e
    steps:
      - uses: actions/checkout@v7

      # Dedicated e2e WIF service account (set as repo vars once provisioned).
      # Runs only on trusted events (same-repo PRs / push / dispatch): fork PRs
      # don't get an OIDC token, so there these steps are skipped and the suite
      # still runs its credential-free specs. On trusted runs a failure is fatal
      # (no continue-on-error) so a green check means the admin-gated specs ran.
      - name: Authenticate to Google Cloud
        if: ${{ vars.E2E_SA != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
        uses: google-github-actions/auth@v3
        with:
          service_account: ${{ vars.E2E_SA }}
          workload_identity_provider: ${{ vars.E2E_WIF_PROVIDER }}
      - uses: google-github-actions/setup-gcloud@v3
        if: ${{ vars.E2E_SA != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}

      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - run: npm ci
      - run: npx playwright install --with-deps chromium

      # Pull the CI admin password from Secret Manager. On trusted runs the SA +
      # grant + secret + e2e_ci user are provisioned, so a failure fails the job
      # rather than silently skipping the admin-gated specs. Fork PRs skip this
      # (guarded above) and run only the credential-free specs.
      - name: Resolve admin password
        if: ${{ vars.E2E_SA != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
        working-directory: .
        env:
          SECRET_NAME: ${{ vars.E2E_ADMIN_SECRET }}
          SECRET_PROJECT: ${{ vars.E2E_ADMIN_SECRET_PROJECT }}
        run: |
          if pw=$(gcloud secrets versions access latest \
                    --secret="$SECRET_NAME" --project="$SECRET_PROJECT"); then
            echo "::add-mask::$pw"
            echo "ADMIN_PASS=$pw" >> "$GITHUB_ENV"
            echo "admin password resolved - admin-gated specs will run"
          else
            echo "::error::$SECRET_NAME not accessible - failing rather than silently skipping the admin-gated specs"
            exit 1
          fi

      - name: Run Playwright e2e
        # Inputs are passed via env (never interpolated into the script) to avoid
        # shell injection through the free-text grep filter.
        env:
          STORE_URL: ${{ github.event.inputs.store_url }}
          ADMIN_USER: ${{ vars.E2E_ADMIN_USER }}
          GREP: ${{ github.event.inputs.grep }}
        run: |
          if [ -n "$GREP" ]; then
            npx playwright test -g "$GREP"
          else
            npx playwright test
          fi
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: screenshots
          path: e2e/screenshots/
          if-no-files-found: warn
